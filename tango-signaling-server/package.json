{
  "name": "tango-signaling-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "proto": "pbjs -t static-module -w es6 -o src/proto/signaling.js ../tango-signaling/src/proto/signaling.proto && pbts -o src/proto/signaling.d.ts src/proto/signaling.js",
    "cf-typegen": "wrangler types",
    "build": "npm run proto && npm run cf-typegen",
    "typecheck": "tsc --noEmit",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "protobufjs": "^7.4.0"
  },
  "devDependencies": {
    "protobufjs-cli": "^1.1.3",
    "typescript": "^5.6.2",
    "wrangler": "^4.90.0"
  }
}
