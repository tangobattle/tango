//! The private gamesupport layer's shared telemetry: the chip-use
//! trackers the game pollers embed. The save/ROM parsing substrate
//! lives in `tango-gamesupport-common-dataview`; the save editor and
//! its supporting cast in `tango-gamesupport-common-ui`.

pub mod telemetry;
