[package]
name = "tango-gamesupport-common"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

# The private gamesupport layer's shared telemetry trackers (the
# chip-use edge detector the game pollers embed). The save/ROM parsing
# substrate lives in tango-gamesupport-common-dataview, the save-editor
# UI in tango-gamesupport-common-ui.

[dependencies]
tango-match = { path = "../tango-match" }

[lints]
workspace = true
