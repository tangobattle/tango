[package]
name = "datachannel-wrapper"
version = "0.1.0"
edition = "2021"

[dependencies]
datachannel = { version = "0.9", features = ["vendored"] }
log = "0.4"
tokio = { version = "1", features = ["full"] }
