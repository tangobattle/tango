/target
# tango-patch is its own workspace root, so it has its own target dir;
# the anchored rule above only covers the one at the repo root.
target/
/dist
/tango_win_workdir
/Tango.app
/tango_macos_workdir

# notes
-*
