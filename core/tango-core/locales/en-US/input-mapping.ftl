input-mapping = Input Mapping
    .prompt = Press input for {$key}.

input-button =
    .left = Left
    .right = Right
    .up = Up
    .down = Down
    .a = A
    .b = B
    .l = L
    .r = R
    .start = START
    .select = SELECT
